{% extends 'base.html.twig' %}

{% block title %}PennarBed — Accueil{% endblock %}

{% block body %}
<section class="home-hero overflow-hidden rounded-3 position-relative">
    <div class="container py-5">
        <div class="row align-items-center gy-4">
            <div class="col-12 col-lg-7 text-lg-start text-center">
                <span class="badge bg-success-subtle text-success fw-semibold mb-2">Association étudiante</span>
                <h1 class="display-5 fw-bold mb-3">PennarBed — Intégration et solidarité</h1>
                <p class="lead text-body-secondary mb-4">Nous aidons les étudiant·e·s d'origine africaine à s'intégrer en France et à réussir leurs démarches administratives.</p>
                <div class="d-flex gap-2 flex-wrap justify-content-center justify-content-lg-start">
                    <a href="{{ path('app_volunteer') }}" class="btn btn-primary btn-lg">Devenir bénévole</a>
                    <a href="{{ path('app_partner') }}" class="btn btn-outline-primary btn-lg">Devenir partenaire</a>
                    <a href="{{ path('app_donation') }}" class="btn btn-success btn-lg">Faire un don</a>
                </div>
            </div>
            <div class="col-12 col-lg-5">
                <img src="{{ asset('images/image-home.jpg') }}" alt="PennarBed — intégration" class="img-fluid rounded-3 shadow home-hero-image" />
            </div>
        </div>
    </div>
</section>

<section class="py-5">
    <div class="row g-4">
        <div class="col-12 col-md-4">
            <div class="feature-card card h-100">
                <div class="card-body">
                    <div class="icon-round bg-primary-subtle text-primary">📘</div>
                    <h3 class="h5 mt-3">Orientation</h3>
                    <p class="mb-0 text-body-secondary">Guides et conseils pour démarrer la vie étudiante en France.</p>
                </div>
            </div>
        </div>
        <div class="col-12 col-md-4">
            <div class="feature-card card h-100">
                <div class="card-body">
                    <div class="icon-round bg-success-subtle text-success">🤝</div>
                    <h3 class="h5 mt-3">Accompagnement</h3>
                    <p class="mb-0 text-body-secondary">Aide concrète: logement, santé, banque, titres de séjour.</p>
                </div>
            </div>
        </div>
        <div class="col-12 col-md-4">
            <div class="feature-card card h-100">
                <div class="card-body">
                    <div class="icon-round bg-warning-subtle text-warning">🌐</div>
                    <h3 class="h5 mt-3">Réseau</h3>
                    <p class="mb-0 text-body-secondary">Mise en relation avec pairs, mentors et partenaires solidaires.</p>
                </div>
            </div>
        </div>
    </div>
</section>

<section class="py-5 border-top">
    <div class="row g-4 align-items-center">
        <div class="col-12 col-lg-6">
            <h2 class="h3 mb-3">Pourquoi PennarBed ?</h2>
            <p class="text-body-secondary">Parce que chaque parcours est unique, nous proposons un accompagnement humain, proche des besoins des étudiant·e·s, avec un réseau de bénévoles et partenaires engagés.</p>
            <ul class="list-unstyled mt-3">
                <li class="mb-2">✔️ Ressources pratiques et ateliers</li>
                <li class="mb-2">✔️ Suivi personnalisé</li>
                <li class="mb-2">✔️ Partenariats locaux</li>
            </ul>
        </div>
        <div class="col-12 col-lg-6">
            <div class="ratio ratio-16x9 bg-light rounded-3 border d-flex align-items-center justify-content-center text-muted">Espace partenaires / événements</div>
        </div>
    </div>
</section>

<section class="py-5 border-top">
    <div class="row justify-content-center">
        <div class="col-12 col-lg-8">
            <h2 class="h3 mb-3 text-center text-uppercase">Contact</h2>
            <p class="text-body-secondary text-center mb-4">Une question, un besoin ? Laissez-nous un message (email et sujet optionnels).</p>

            {% for label, messages in app.flashes %}
                {% for message in messages %}
                    <div class="alert alert-success">{{ message }}</div>
                {% endfor %}
            {% endfor %}

            {{ form_start(contactForm) }}
                <div class="row g-3">
                    <div class="col-12 col-md-6">{{ form_row(contactForm.email) }}</div>
                    <div class="col-12 col-md-6">{{ form_row(contactForm.subject) }}</div>
                    <div class="col-12">{{ form_row(contactForm.message) }}</div>
                </div>
                <div class="mt-3 text-center">
                    <button class="btn btn-primary">Envoyer</button>
                </div>
            {{ form_end(contactForm) }}
        </div>
    </div>
</section>
{% endblock %}



