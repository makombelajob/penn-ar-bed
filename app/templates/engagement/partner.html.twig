{% extends 'base.html.twig' %}

{% block title %}Devenir partenaire — PennarBed
{% endblock %}

{% block body %}
	<section>
		<div class="text-center">
			<h1 class="h3 mb-3 text-uppercase">Devenir partenaire</h1>
			<p class="text-muted">Entreprises, associations, institutions: soutenez notre action.</p>
		</div>
		<div class="col-11 col-md-9 col-lg-6 m-auto">
			{% for label, messages in app.flashes %}
				{% for message in messages %}
					<div class="alert alert-success">{{ message }}</div>
				{% endfor %}
			{% endfor %}

			{{ form_start(form) }}
			{{ form_row(form.organization) }}
			{{ form_row(form.email) }}
			{{ form_row(form.partnershipType) }}
            <div class="text-center">
			    <button class="btn btn-outline-primary">Nous contacter</button> 
            </div>
			{{ form_end(form) }}

		</div>
	</section>
{% endblock %}
